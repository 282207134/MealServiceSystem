# Storage 存储桶配置完成

## ✅ 已完成的配置

### 1. 存储桶创建
- **名称**: `product-images`
- **类型**: Public（公开访问）
- **文件大小限制**: 50MB
- **允许的文件类型**: 
  - image/jpeg
  - image/png
  - image/webp
  - image/gif

### 2. 存储策略配置

#### ✅ 读取策略
- **策略名**: "所有人可以读取图片"
- **权限**: SELECT
- **角色**: public（所有人）
- **说明**: 允许所有人读取存储桶中的图片

#### ✅ 上传策略
- **策略名**: "认证用户可以上传图片"
- **权限**: INSERT
- **角色**: authenticated（认证用户）
- **说明**: 只有登录用户才能上传图片

#### ✅ 删除策略
- **策略名**: "所有者可以删除图片"
- **权限**: DELETE
- **角色**: authenticated（认证用户）
- **说明**: 只有文件所有者才能删除自己上传的图片

## 📋 配置验证

存储桶和策略已通过 SQL 验证，配置正确。

## 🚀 使用方式

在代码中使用存储桶：

```dart
import 'package:supabase_flutter/supabase_flutter.dart';

final supabase = Supabase.instance.client;

// 上传图片
await supabase.storage
  .from('product-images')
  .upload('path/to/file.jpg', file);

// 获取公开 URL
final imageUrl = supabase.storage
  .from('product-images')
  .getPublicUrl('path/to/file.jpg');
```

## 📝 注意事项

1. **文件大小限制**: 单个文件最大 50MB
2. **文件类型限制**: 仅支持图片格式（jpeg, png, webp, gif）
3. **权限控制**: 
   - 所有人可以查看图片（Public bucket）
   - 只有登录用户可以上传
   - 只有文件所有者可以删除

## 🔒 安全建议

如需更严格的权限控制，可以考虑：
- 创建私有存储桶用于敏感图片
- 添加更细粒度的上传路径限制
- 添加文件大小和类型验证

